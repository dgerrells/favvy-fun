# Favy

A simple favicon game.

## Build

nothing, it is static files. Just run the server.

## Processing

`scripts` folder contains various scripts for processing top 1m websites csv files with simple rules, then enhancing categories with gemini, and finally building a database. Fav icons are lazily initialized by the server and cached. 

## Deploying

Run the bun server which spins up the app.